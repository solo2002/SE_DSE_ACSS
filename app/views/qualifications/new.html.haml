%h2 Select from #{@round.round_name}

= form_tag competition_round_qualifications_path, :method => :post do
	%table
		%tr
			%th Name
			%th Contestant Number
			%th Select
		- @participants.each do |participant| 
			%tr
				%td= participant.p_name
				%td= "To be given"
				%td= check_box_tag 'participant_id[]', participant.id

	- @rounds.each do |round|
		= check_box_tag 'r_id', round.id
		= round.round_name
	= submit_tag 'Submit'



